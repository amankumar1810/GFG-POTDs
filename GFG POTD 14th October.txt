// Count Linked List Nodes(Basic)

class Solution {
  public:
    // Function to count nodes of a linked list.
    int getCount(struct Node* head) {
        // Sabse basic Question.
        int count = 0;
        Node *temp = head;
        while(temp){
            count++;
            temp = temp->next;
        }
        return count;
    }
};